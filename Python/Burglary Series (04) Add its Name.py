# Given three arguments - a dictionary d1, a key name and a value
# return an object with that name and value in it (as key-value pairs).

def add_name(obj, name, value):
    obj[name] = value
    return obj
